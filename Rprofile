r = getOption("repos")           
r["CRAN"] = "https://cran.rstudio.com/"
options(
	repos = r,
	prompt = "================================================================================\n» "
)

coltam <- function(){
	theme_minimal(
		      base_size = 12,
		      base_family = "Encode Sans"
		      ) +
	theme(
	      panel.grid = element_blank(),
	      legend.position = "top"
	      )
}
